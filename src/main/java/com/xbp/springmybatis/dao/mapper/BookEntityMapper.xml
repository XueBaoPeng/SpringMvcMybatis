<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xbp.springmybatis.dao.BookEntityMapper">
    <resultMap id="BaseResultMap" type="com.xbp.springmybatis.entity.BookEntity" >
        <constructor >
            <idArg column="book_id" jdbcType="INTEGER" javaType="java.lang.Integer" />
            <arg column="name" jdbcType="VARCHAR" javaType="java.lang.String" />
            <arg column="number" jdbcType="INTEGER" javaType="java.lang.Integer" />
        </constructor>
    </resultMap>

    <!-- 目的：为dao接口方法提供sql语句配置 -->
    <select id="queryById" resultMap="BaseResultMap" parameterType="long">
        SELECT book_id, name,number
        from book
        where book_id=#{bookId}
    </select>

    <select id="queryAll" resultMap="BaseResultMap">
        select book_id,name,number
        from book
        order by book_id
         LIMIT #{offset},#{limit}

    </select>
    <update id="reduceNumber">
		UPDATE book
		SET number = number - 1
		WHERE
			book_id = #{bookId}
		AND number > 0
	</update>

</mapper>